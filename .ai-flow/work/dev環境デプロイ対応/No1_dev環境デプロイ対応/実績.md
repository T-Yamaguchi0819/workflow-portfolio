# 実績 No1: dev環境デプロイ対応

```yaml
task: "dev環境デプロイ対応"
no: 1
completed: 2026-08-14
size: M
risk: Critical
human_hours_actual: null
human_hours_baseline: null
ai_hours: null
first_pass: true
rework_count: 0
ai_findings_effective: 2
human_findings: 0
escalations: 0
flow_completed: true
```

## 指標の定義

| キー | 意味 |
|------|------|
| human_hours_actual | AI利用時に人間が実際に使った時間（指示・確認・レビュー含む） |
| human_hours_baseline | AIを使わなかった場合の想定時間（担当者見積もり）。削減率はこの2つから算出される |
| ai_hours | AI実行時間（参考値） |
| first_pass | 最初の[6]動作確認で全観点PASSしたか |
| rework_count | Plan/テスト/実装への差し戻し回数（レビュー往復含む） |
| ai_findings_effective | [5]レビューの指摘のうち実際に修正につながった数 |
| human_findings | **AIレビュー後に人間が発見した問題数**（重要指標。PR の人間レビュー指摘を含む。内容は下に記載） |
| escalations | 🛑 エスカレーション（レビュー2往復超・テスト3回失敗等）の発生回数 |
| flow_completed | フローを完遂したか。途中で従来手順に切り替えた場合は false＋理由を特記事項へ |

## 実績サマリ

- 2026-08-14 の 1 日で [0]初期化〜[8]完了まで完遂（[3]テスト先行はユーザー承認済みスキップ。アプリコード変更なしのため、チェンジセット確認＋デプロイ内蔵スモーク＋[6]で代替）
- [5]独立レビュー: **初回 PASS（往復 0）**。要修正 0 件、推奨 2 件（github-oidc.yaml / deploy.yml のコメント・説明文の実態乖離）は同フェーズ内で対応済み → ai_findings_effective = 2
- [6]動作確認: **初回 PASS（テスト失敗 0）**。mvn test 16 件全 PASS、dev API CRUD/検索/エラー形式、dev フロント SSR、prod 回帰（データ・URL 不変・テーブル分離）を確認
- 人間（ユーザー）からの追加指摘: 現時点 0 件 → human_findings = 0（マージ後に人間レビュー指摘があれば本ファイルへ追記し、失敗事例を precedents.md へ記録する）
- エスカレーション: 0 回
- コミット: 現時点 1 件（`5fe7b2e`、[5]後にユーザー明示指示で実施済み。commit_policy=manual）。[6][7] 成果物・ドキュメント変更・precedents 追記・本ファイル等は未コミットで、要否は完了ゲートで確認

## 人間による追加指摘の内容（human_findings > 0 の場合は必須）

- 該当なし（0 件）。マージ後の人間 PR レビューで指摘が出た場合はここへ追記する（8-finalize 手順 4）

## 特記事項

- **null とした項目の理由**（推測値の記入は禁止のため）:
  - `human_hours_actual`: 人間の実働時間（指示・確認・承認）が未計測。完了ゲートでユーザーから得られれば追記する
  - `human_hours_baseline`: AI を使わない場合の想定工数は担当者見積もりが必要で未取得。同上
  - `ai_hours`: AI 実行時間の計測なし（参考値のため null 許容）
- **S/M/L 判定の妥当性**: M は適切だった（変更 5 ファイル＋AWS 側設定、アプリコード変更なし、影響はデプロイ基盤に閉じた。実績もこの範囲を超えていない）
- **リスク Critical の運用**: OIDC 信頼条件拡張・DynamoDB +10RCU/10WCU が該当。独立レビュー[5]＋各ゲートでの人間承認（AC・Plan・[5]通過・[7]前）を経ており、フェーズ跳ばしなし
- **フロー上の気づき**:
  - [4]実装エージェントが SAM CLI を「未導入」と誤検出（実際は導入済み 1.152.0）→ precedents.md 失敗事例 **F-008** に記録
  - prod 無置換は `sam deploy --no-execute-changeset` のチェンジセット実機確認で検証（precedents P-001）。OIDC StringLike の完全一致評価（P-002）、Amplify 自動ブランチ作成の発火条件（P-003）も前例化
- **後続への申し送り**（verify.md §7。本タスクの AC 未検証 2 項目＋任意 1 項目）:
  1. main マージ時の prod デプロイ成功確認（Actions run・prod スモーク・URL 不変）
  2. Amplify ブランチ自動削除の実挙動確認（`ai/dev-env-deploy` 削除時）→ 結果をドキュメントへ反映
  3. 多階層ブランチ（`ai/a/b`）の Amplify 自動作成は任意項目・未検証（次に使う機会に確認して記録）
- dev テーブルに検証用記事 1 件を意図的に残置（verify.md §6 に判断記録。タイトルで検証用と明示済み・コスト影響なし）
