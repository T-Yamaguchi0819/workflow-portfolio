<!-- テンプレート: [1]要件確認で `No{N}_{機能名}/要件.md` としてコピーして埋める。
     {}: 置換して記入。該当しない節は削除せず「なし」と記載（記載漏れと区別するため）。
     規模Sの簡易版では「概要」「期待動作」「Acceptance Criteria」のみ必須。 -->
# 要件 No{N}: {タイトル}

## 基本情報

| 項目 | 内容 |
|------|------|
| 要件No | No{N} |
| 種別 | {不具合修正／改善・機能変更} |
| モード | {資料あり（資料パス）／資料なし（チケットNo・指示元）} |
| 対象画面/機能 | {} |
| リリース時期 | {あれば／なし} |

## 概要

{要件資料の要約／ユーザー報告の要約}

## 詳細要点

- {変更内容の要点。資料の精読結果}

## 期待動作（正しい仕様）

- {何がどう動けば正しいか。ユーザーと認識合わせ済みの内容のみ確定させる}

## Acceptance Criteria（承認後は本節が正。変更には再承認が必要）

<!-- [3]テスト・[6]動作確認・[5]レビューの唯一の判定基準。
     AIが書くテストは本節を満たすための実装物であり、本節の根拠は常に人間の承認 -->

### 期待結果

- {条件 → 期待される結果 の形式で列挙}

### 境界値

- {境界での期待挙動。なければ「なし」}

### 異常系

- {エラー・不正入力時の期待挙動。なければ「なし」}

### 禁止事項（この修正でやってはいけないこと）

- {project.yaml の constraints 由来＋本件固有の禁止。例: ○○のレスポンス形式を変えない}

### 変更してはいけない既存仕様

- {コード上は不要・冗長に見えても外部仕様として維持するもの。なければ「なし」}

### 承認記録

- 承認者: {ユーザー} ／ 承認日: {YYYY-MM-DD} ／ 再承認履歴: {なし}

## 不具合情報（不具合修正の場合のみ。改善なら「該当なし」）

- 事象（実際の動作）: {}
- 再現手順: {}
- 発生条件（権限・データ条件・環境）: {}

## 参考実装（指定された場合のみ。なければ「なし」）

- {参考画面・箇所。報告文言と参考実装に差異がある場合、どちらを採用したかを明記}

## 参照資料

- {別紙・関連ドキュメントのパスと取り込んだ要点。なければ「なし」}

## 未解決の確認事項

- {ユーザー確認待ちの点。なければ「なし」}
