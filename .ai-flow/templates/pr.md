<!-- テンプレート: git.commit_policy: auto の [8] で PR 本文として使う。
     {} を埋め、gh pr create --body-file 等で発行する。
     成果物の本文は転記しない（正はブランチ上のファイル。参照で足りる）。 -->
# {タスク名}

## 概要

{修正内容の要約。**Why**（なぜこの変更が必要か＝背景・目的）を必ず含める}

## 要件・Acceptance Criteria

{要件の要約（No ごと）。詳細はブランチ上のファイルを参照}

- 要件.md: `{work_dir}/{タスク名}/No{N}_{機能名}/要件.md`（AC 承認日: {YYYY-MM-DD}）

## リスク・規模・変更分類

| 項目 | 値 |
|------|-----|
| 規模 | {S／M／L} |
| リスク | {Low／Medium／High／Critical} |
| 変更禁止への抵触 | {なし／あり（内容）。plan.md の変更分類に準拠} |

## 検証

- 動作確認: {verify.md の要約とエビデンスパス}
- 独立レビュー（[5]）: {指摘件数と対応状況。review.md 参照}
- AI PR レビュー: {実施済み（PR コメント参照）／未実施（pr.ai_review: false）}

## 人間レビュアーへ（レビュー分担）

コードの正しさ（AC への反例・変更禁止侵害・スコープ外変更）は AI レビュー層（[5]独立レビュー＋AI PR レビュー）が検査済みです。**このレビューでは AI に判定できない以下に集中してください**:

- [ ] AC 自体が業務要件として妥当か
- [ ] 外部仕様・関連システムとの整合（AI が知らない文脈がないか）
- [ ] {リスク High 以上の場合のみ} 変更箇所の精読（Low/S はサマリ確認のみで可）

## 成果物

- 状態・成果物一式: `{work_dir}/{タスク名}/`

{issue_sync 有効時のみ（無効なら本行を削除）}
Closes #{Issue番号}
