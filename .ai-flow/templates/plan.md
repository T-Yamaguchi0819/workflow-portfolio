<!-- テンプレート: [2]Planで `No{N}_{機能名}/plan.md` としてコピーして埋める。
     ①〜⑧の見出しは削除・改変しない（下流フェーズ・レビュアーが参照する）。該当なしは「なし」と記載。
     規模Sの簡易版では ②③⑤⑥ のみ必須。 -->
# 修正Plan No{N}: {タイトル}

## ① 要件（要約）

{要件.md からの要約。AC の要点を含める}

## ② 対象ファイル一覧

| # | ファイル（フルパス） | 区分 |
|---|---------------------|------|
| 1 | {} | {画面／ロジック／データアクセス／設定 等} |

## ③ 変更方針（ファイルごとに具体的に）

### {ファイル名}

- {何をどう変えるか。踏襲する既存パターン・参考実装があれば明記}

## ④ 影響範囲（依存レイヤの連鎖を明示する）

- 依存レイヤ連鎖: {例) Controller → Service → Repository。今回の変更がどの層まで届くか}
- 呼び出し元: {}
- 関連画面・機能: {}
- DB（テーブル・ビュー）: {}
- 権限・ロールによる差異: {}

## ⑤ 変更分類（レビュー[5]の必須確認項目）

<!-- 特に DB項目・外部I/F・エラーメッセージ・権限・ログ・トランザクション・既存の特殊仕様は
     「変更禁止」「要確認」への分類を明示的に検討する（不要に見えて外部仕様のものが多い） -->

| 分類 | 対象 |
|------|------|
| 変更対象（今回修正するもの） | {} |
| 変更可能（触れても影響が閉じるもの） | {} |
| **変更禁止**（外部仕様・共有資産） | {} |
| 要確認（判断できずユーザー確認が必要） | {なければ「なし」} |

## ⑥ リスク判定・規模判定

| 項目 | 判定 | 根拠 |
|------|------|------|
| リスク | {Low／Medium／High／Critical} | {project.yaml の risk 分類のどれに該当するか} |
| 規模 | {S／M／L} | {} |
| 独立レビュー[5] | {必須（High以上 or M/L）／省略提案} | {} |
| 人間レビュー | {必須（Critical）／不要} | {} |

## ⑦ 動作確認の観点（[3]テスト・[6]動作確認の基準）

- 正常系: {}
- 回帰（④影響範囲を引き継いだ他機能への影響確認）: {}
- 異常系: {}
- 境界値: {}
- 権限別: {}

## ⑧ ドキュメント反映方針（docs.enabled の場合のみ。無効なら「対象外」）

### 前例調査（必須）

- 参照した前例・記載慣行: {knowledge/precedents.md の項目名／調査したドキュメントと結果}

### 反映要否

| ドキュメント | 要否 | 根拠（該当記載・前例） |
|-------------|------|----------------------|
| {docs.targets の各対象} | {要／不要} | {} |

<!-- すべて「不要」の場合は [7] スキップ候補（スキップはユーザー承認が必要） -->
